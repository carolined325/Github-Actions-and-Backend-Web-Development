name: Flask
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  Flask:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: pip install flask
